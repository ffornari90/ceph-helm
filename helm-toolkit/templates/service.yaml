apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-toolkit.fullname" . }}
  labels:
    {{- include "helm-toolkit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-toolkit.selectorLabels" . | nindent 4 }}
