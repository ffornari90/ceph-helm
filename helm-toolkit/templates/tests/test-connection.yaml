apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-toolkit.fullname" . }}-test-connection"
  labels:
    {{- include "helm-toolkit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-toolkit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
